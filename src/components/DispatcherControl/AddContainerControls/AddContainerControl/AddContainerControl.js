import React from 'react';

const addContainerControl = (props) => (
    <div className={classes.AddContainerControl}>
        <button>Add Container</button>
    </div>
)

export default AddContainerControl;